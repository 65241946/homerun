# Homerun 策略修复施工规格书(两批全量)

用途:交给实施会话照单施工;我(规划会话)事后从 GitHub 按本文档的验收标准逐条检查。
配套背景文档(项目「策略」里已有,含全部 file:line 依据):《策略优化方案_信号新闻跟单类》《策略优化方案_加密高频BTC_ETH》。
行号基于 `claude/batch-2-identity-finality-review-jvcsc5` 分支的 seed 模板;实施前先 `git pull` 对齐。

---

## 施工总则(所有工单共同遵守)

1. **分支与提交**:每批一个分支(建议 `fix/batch-a-signal-copytrade`、`fix/batch-b-crypto-hf`);每个工单一个或一组 commit,message 前缀工单号(如 `WO-B1: ...`)。
2. **单一真值源原则**:任何 `params.get(key, 魔法数)` 的 inline 回退值,必须与 `default_config` 完全一致(或直接读合并后的 config)。新增 key 必须同时进 `default_config` + config schema(带 description)。
3. **不引入新依赖**;沿用现有代码风格;改完对每个触碰文件跑 `python3 -m py_compile`。
4. **验收 grep**:每个 `default_config` key 在文件中出现 ≥2 次(定义一次 + 至少一处读取)。零死配置。
5. **单元测试**:每个工单的验收点写进 `backend/tests/test_<strategy>_repair.py`;纯逻辑测试,不依赖 DB/网络(必要时提取纯函数)。
6. **行为变更日志**:凡默认值发生变化的 key,在 PR 描述里列成表(旧值→新值→理由),供 shadow A/B 对照。
7. **部署提醒**:这些文件是 SEED TEMPLATE,合并后必须把 DB `strategies.source_code` 同步(UI 策略编辑器或 API),否则线上不生效。
8. **禁止虚构**:工单里写明的公式/默认值按规格实现;实施中发现规格与代码现实冲突,停下来在 PR 里记录,不要自行发明替代方案。

---

# 批次 A:信号 / 新闻 / 跟单类

## WO-A1 `traders_copy_trade.py`(完整版,含 P0 验收)

> 注:P0 部分可能已有另一分支在修;实施前先检查是否已合并,已合并的项只做验收不重做。参考草稿 patch:`traders_copy_trade_p0_draft.patch`(会话产物,可要)。

1. **copy_delay/max_age 死锁**(P0):`validate_traders_copy_trade_config` 中,若 `copy_delay_seconds >= max_signal_age_seconds`,则 `max_signal_age_seconds = min(600, copy_delay_seconds + 5)`,并加注释说明窗口语义 `[copy_delay, max_age]`。
2. **edge 公式替换**(P0):删除 `abs(0.5-p)*200`。新公式:
   `structural_upside_pct = (1-p)/p*100(0<p<1,否则 0)`
   `edge_percent = min(structural_upside_pct, edge_max_percent) * max(edge_conviction_floor, confidence)`
   新 key:`edge_conviction_floor=0.15`、`edge_max_percent=100.0`(入 default_config + schema)。旧 `edge_midpoint/edge_multiplier` key 保留但 schema 标注 deprecated、不再驱动核心。
3. **confidence 缺省统一**(P0):detect(`_build_copy_opportunity`)与 evaluate 缺省都用 0.45(= `min_confidence` 默认),消除 0.70/0.0 分裂。
4. **风控默认值收紧**:`leader_allocation_cap_pct` 100→**25**;`max_copy_drawdown_pct` 100→**50**。`max_copy_daily_loss_usd` 等三个 1,000,000 的预算保留数值(账户规模未知,不虚构),但 schema description 加 **"上线实盘前必须按账户规模设置"**。
5. **resolution_date 造假**:删除 `detected_at + 15min`;用市场真实 `end_date`,取不到就不设该字段。
6. **fail-open 可配**:新 key `require_live_context: bool = False`。为 True 时,`live_market` 缺 `liquidity_usd`/`entry_price_delta_pct` 的信号直接拒绝(检查名不变、pass 逻辑按配置切换)。schema 注明 live 模式建议 True。
7. **热路径**:`validate_traders_copy_trade_config` 移入 `configure()` 缓存(config 变更时重算);`_to_utc(timestamp)` 每信号只解析一次。
8. **卖出镜像验证**(不新增逻辑,补测试):单测证明 `copy_sells=True` 时,leader 卖出事件生成的 sell opportunity 能通过 inventory 检查并对应减仓路径。

**验收**:grep 全 key ≥2;单测:(a) delay=60/max_age=5 配置校验后窗口非空;(b) edge 数值表:p=0.99→≈1.01×conv、p=0.50→100×conv(封顶生效)、p=0.10→100(cap);(c) confidence 缺省两路径一致;(d) 卖出镜像。

## WO-A2 `traders_confluence.py`

1. **方向不明丢弃**(P0):`_resolve_trade_outcome`(:163)无明确方向时返回 `None`,调用方跳过该信号(不再默认 "NO")。
2. **回退值统一**:`min_confluence_strength` 回退 0.55→0.50(:801);`min_tier` 回退 "high"→"low"(:417)。与 DEFAULT_CONFIG 对齐。
3. **`firehose_max_age_minutes` 720→60**(行为变更,记日志)。
4. **tier 加权汇聚**:新 key `tier_weights = {"low":1.0,"medium":1.5,"high":2.0,"extreme":3.0}`;有效钱包数 = Σ tier_weight(优先用 cluster_adjusted 名单);`min_wallet_count` 改为对加权和判定(schema 说明)。tier 同时进入 score(替代现在只微调 risk 的做法)。
5. **重复计算去除**:`build_opportunities_from_firehose` 不再对已过滤行重跑 `evaluate_firehose_signal`(:570-571);`normalize_trader_signal` 每行至多一次(传递已归一化结果);`_effective_config()` 按 config 版本 memoize。
6. **实例态竞态**:`self._confluence_strength`(:816)改为经 payload/params 传递到 compute_score/compute_size。

**验收**:单测:方向不明→无信号;加权 wallet_count 计算;回退值 grep 与 DEFAULT_CONFIG 一致;同一信号单遍 evaluate(可用调用计数 stub)。

## WO-A3 `news_edge.py`(+ `news/edge_estimator.py`、`news/edge_detector.py`)

1. **估计器统一**:以 `edge_estimator.py` 为唯一估计器(它有 novelty/超时/相关性过滤)。`detect_async` 路径改为调用 estimator;`edge_detector.py` 保留兼容壳或删除(二选一,PR 说明)。所有 LLM 调用统一 `asyncio.wait_for(20s)`。
2. **age=0 truthiness**(P0):`_extract_signal_age_minutes`(:290)改 `is not None` 判定链。
3. **发出前刷新价**:`_edge_to_opportunity` 前用 `StrategySDK.get_live_price(token_id)` 重取现价、以现价重算 edge;刷新后 edge < 阈值 → 丢弃(计数入日志)。
4. **CI 闸门**:新 key `require_ci_clears_market: bool = True`——要求模型概率的 CI(现有 `_resolve_uncertainty` 输出)整体在市场价的正确一侧;另加收缩 key `llm_shrinkage_k=0.7`:`adj_prob = market + k*(model-market)`,edge 用 adj_prob 计算(两道可独立开关)。
5. **费后闸**:`fee_adjusted_edge_pct` 移到 min_edge 判定之前,闸门作用于费后值。
6. **时间衰减**:`edge_eff = edge * exp(-ln2 * age_minutes / half_life)`;新 key `edge_half_life_minutes_by_category`(默认 `{"default": 45}`),category 取 estimator 已有字段。
7. **阈值单源**(P0):evaluate 回退 3.0/0.42/0.68 全部改读 `NEWS_EDGE_DEFAULT_CONFIG`。
8. **LLM 缓存**:结果按 `(article_id, market_id, round(price,2))` 缓存,TTL ≥ 3 个扫描周期;embedding 只算新文章(按 article_id 记已嵌入集合)。
9. **score 重写**:`score = fee_adj_edge_pct * 1.0 + confidence * 10`(注释:权重待回测校准;替换 edge*0.55+conf*45 的 conf 主导版)。

**验收**:单测:(a) age=0 正确读取;(b) 衰减半衰期数值;(c) CI 跨市场价 → 拒;(d) 缓存命中不再调 LLM(stub 计数);grep 阈值单源。

## WO-A4 `news_momentum_breakout.py`

1. **相对突破阈**:新 key `breakout_threshold_rel=0.25`(rise/baseline ≥ 25%);原 `breakout_threshold`(绝对 0.10)保留为下限兜底,rel 优先。schema 说明两者关系。
2. **量纲统一**:出场统一 pct 族(`take_profit_pct/trailing_stop_pct`);`scale_out_config` 的 bps 触发(30/60bps)与 pct 无法共存——按 pct 重设(建议 targets 于 +25%/+45% 各减 1/3,trailing 12%),PR 行为变更表必列。
3. **stall-exit 改回吐制**:触发条件改 `price <= high - stall_giveback_fraction*(high-entry)` 且无新高超 `momentum_stall_minutes`;新 key `stall_giveback_fraction=0.5`。删除「价格≤entry 才触发」。
4. **min_liquidity 单源**:custom_checks 回退 1500 删除,读 config(3000)。
5. **history 修正**:deque `maxlen = ceil(stale_history_seconds / 5)`(按 5s 刷新估计,注释);每 detect 周期对 `price_history/last_emit` 做市场 key GC(last-seen 超过 `stale_history_seconds*2` 清除)。
6. **正则预编译**:`_keyword_in_text` 的 pattern 类加载期编译缓存;`compute_score` 改读 `scoring_weights` 数据类(删除 inline 复制品)。
7. **`target_distance_to_one_fraction` 0.55→0.35**(降低目标虚高;标注回测校准)。

**验收**:单测:相对阈在 0.20/0.70 两价位的对称性;stall 回吐触发;deque 尺寸;key GC。

## WO-A5 `certainty_shock.py`

1. **词边界匹配**(P0):把 news_momentum 的 `_keyword_in_text` 抽为共享 util(或等价复制,注明来源),替换 `if kw in text`(:257)。
2. **近因约束**(修「漂移当冲击」):新 key `shock_recent_window_seconds=900`、`shock_recent_share_min=0.6`——要求总 move 的 ≥60% 发生在最近窗口内(模式参照 news_momentum 的 `min_5m_share_of_30m`,同 repo 已有先例)。
3. **side 价格序列**:history 存 `(ts, yes_price, no_price)`;NO 方向的 move/retrace/target 用 no 序列。
4. **死区对齐**:config 校验强制 `shock_min_expected_move >= min_edge_percent/100`。
5. **风险闸生效**:risk 公式加流动性与临期项使其可超 0.70,或将 `max_risk_score` 默认降至 0.60(二选一,PR 说明)。
6. **热路径**:deadline 按市场缓存;`import calendar` 移顶层;history 仅在通过 deadline 检查后累计;市场 key GC(同 A4)。

**验收**:单测:"whether/resolution" 不再被 eth/sol 命中;6 小时匀速漂移(近因share<0.6)拒、末 10 分钟集中重定价过;NO-side 序列正确性。

---

# 批次 B:加密高频(BTC/ETH 引擎 + 5 小策略)

## WO-B0 共享层(先做,B1-B4 依赖)

1. **费用统一**:`strategies/crypto_strategy_utils.taker_fee_pct`(:100-102)改为 `from utils.kelly import polymarket_taker_fee_pct` 的委托/别名,删除重复公式体。全部加密策略经此取费。
2. **timeframe-aware 门槛统一**:所有策略的 oracle-age / market-data-age / min-seconds-left 默认值走 `default_max_oracle_age_ms / default_max_market_data_age_ms / default_min_seconds_left_for_entry`(`strategies/crypto_strategy_utils.py:344-376`),策略 config 只做覆盖。
3. **clamp/gate 约定**:任何 confidence clamp 的下限统一 **0.30**,必须低于该策略默认 `min_confidence`(写进共享注释)。
4. **共享概率 helper**:新增 `estimate_p_win(diff_pct, elapsed_ratio, *, base_scale, min_scale, prob_min, prob_max)`:
   `scale = max(min_scale, base_scale * (1 - elapsed_ratio))`
   `p = clamp(bounded_sigmoid(diff_pct / scale), prob_min, prob_max)`
   (`bounded_sigmoid` 已存在 :105)。供 B1/B2 用。

## WO-B1 `btc_eth_directional_edge.py`(核心工单)

1. **接线 `directional_*` 全块**(:615-631,当前全死):用 WO-B0 的 `estimate_p_win`(参数即 `directional_base_scale/min_scale/oracle_prob_min/max`);`edge_percent = (p_win − entry_price)*100`(概率点);phase 由 elapsed_ratio 切 early/mid/late,分别应用 `directional_early/mid/late_min_edge` 闸与 `*_score_mult`;score = `min(directional_max_score, edge * directional_edge_score_scale / 100 * phase_mult) + (late? directional_late_phase_bonus : 0)`,替换 `edge*0.7+conf*30`(:2833)。
2. **费用闸**:`min_execution_adjusted_edge_percent` 默认 0→**0.5**;net_edge = edge − `fee_aware_min_edge_pct(entry_price, multiplier=2.0)`;`skip_fee_model=True` 移除或注明由本闸替代。
3. **阈值收敛与重定标**:`min_edge_percent` 默认 3.0→**1.5**(现在量纲是概率点,费后另有闸);detect 的 `min_oracle_move_pct` 降为粗筛(默认 0.15,per-tf 可覆盖,进 default_config);删除 evaluate 里的第二份 0.15(:1987-1991)与 `quality_filter_overrides(min_roi=1.0)` 的量纲冲突(custom_roi 用新 edge)。
4. **修活 guardrail**:detect 把 `model_prob_yes/no`、`up_price/down_price` 写入 `_crypto_context`(:4419-4459),使 :1141-1148/2071-2091 的方向 guardrail 真正生效。
5. **per-tf 门槛**:oracle age 12s 平坦值(:1757)、min_seconds_left inline 表(:1392-1397)全部走 WO-B0 helper。
6. **sizing**:删除反向 edge hack(:2848-2855 的 15/50/60/0.3 常数);改 `StrategySDK.fractional_kelly_size`(quarter-Kelly)基于 p_win vs entry_price,受 `max_size` 与风控 cap 约束。
7. **熔断入 config**:`consecutive_loss_pause_minutes=15`、`consecutive_loss_pause_enabled=True`(:3989/4001)进 default_config + schema。
8. **热路径**:decision_payload 懒构建(仅 selected 或 `debug_decision_payload=True`);asset/timeframe 按 market_id 缓存;`get_book_imbalance` 按 (token, tick) memo;`_edge_first_seen_ms`/`_last_selected_at_ms_by_market` 加 TTL 清理(如 10 分钟)。
9. **死代码**:`_direction_allowed` 的 elapsed 机器(:997-1046)——接线(两个 enabled 默认改 False 则机器有意义)或删除,PR 说明。

**验收**:`directional_*` 全 key grep≥2;单测:p_win 对 diff 单调、对 elapsed 的 scale 衰减(同 diff 晚期 p 更极端)、费用闸拒/过边界、Kelly 尺寸数值表;`decision_payload` 在 skip 路径不构建(mock 计数)。

## WO-B2 `btc_eth_convergence.py`(实现文档宣称的收敛模式)

> 决策依据:latency-arb 已由 directional 文件承担;本文件按其 docstring(:6-14,522-532)实现真收敛模式,消除名实不符。

1. **接线 `convergence_*` 全块**(:601-607):入场要求 `seconds_left ∈ [convergence_min_seconds_left, convergence_max_seconds_left]`(文档语义:最后 5–45s)、entry ∈ `[convergence_min_price, convergence_max_entry_price]`(0.85–0.95 区)、`|oracle_diff_pct| ≥ convergence_min_oracle_diff_pct`;**方向 = oracle 有利侧(买 favored/贵的一边)**。
2. **移除 cheap-side 逻辑**:repricing ceiling(0.50+0.30,:4152-4171)与 min_seconds_left 大表(:1360-1379)属 latency 模式,对本模式删除/替换为上面窗口闸。
3. **oracle 新鲜度硬闸**(修 fail-open):detect 路径 `oracle_age_ms ≤ default_max_oracle_age_ms(timeframe)`;age 缺失视为不新鲜、拒绝(:1894-1895 的宽松分支对本模式关闭)。
4. **出场**:保留 binary_resolution_hold;新增 oracle 反转平仓——`sign(oracle_diff_pct)` 翻转且 `seconds_left > convergence_min_seconds_left` 时 flatten(新 key `exit_on_oracle_flip=True`)。
5. **评分与费用**:score = `convergence_base_score + g(|diff|)`,cap `convergence_max_score`;费用按曲线计入 edge(0.85–0.95 区费用小但非零);`skip_fee_model` 移除。
6. **sizing**:Kelly on `p_win`(WO-B0 helper,临期 scale→小,p 趋极值)vs entry。
7. **同 B1 的**:熔断入 config、懒 snapshot、缓存、utcnow 单次;`_market_ml_probability_yes` 只升不降的单侧先验(:4218-4228)改为对称(近 0.5 的 ML prob 应降低 confidence)。

**验收**:场景单测:(a) 剩 30s、favored 0.90、fresh、diff≥min → emit 且方向= favored;(b) 剩 300s → 不 emit;(c) oracle age 缺/超限 → 不 emit;(d) 持仓中 diff 翻转 → flatten。`convergence_*` 全 key grep≥2。

## WO-B3 `btc_eth_maker_quote.py`

1. **接线 `maker_quote_*` 全块**(:585-596):
   - `maker_quote_min_spread`:spread 低于此不报价(主闸,替代方向性 min_edge 作为 maker 主闸)。
   - **skew**(修 §4.1 命门):`s = clamp(oracle_diff_pct / skew_scale, -1, 1) * maker_quote_skew_max`;favored 腿报价 +s(更近 touch)、另一腿 −s;新 key `skew_scale=1.0`(1% diff → 满 skew)。腿名义可按 `(0.5+s/2):(0.5−s/2)` 加权(PR 文档化)。
   - 腿尺寸 clamp `[maker_quote_min_size_usd, maker_quote_max_size_usd]`。
   - `maker_quote_min_seconds_left`:低于则不报价;`session_timeout = min(session_timeout_cfg, seconds_left − maker_quote_resolution_risk_seconds)`(修临到期裸挂)。
   - 评分:`maker_quote_base_score + spread*maker_quote_spread_score_scale + (book<thin_book_usd ? thin_book_bonus:0)`,cap `max_score`。
2. **组合成本闸**:新 key `maker_min_combined_edge=0.015`——要求 `quote_yes+quote_no ≤ 1 − maker_min_combined_edge`(替换 0.998 硬数);补腿需跨价时按费用曲线预估成本计入。
3. **库存一致性**:`max_unhedged_notional_usd = leg_fill_tolerance_ratio × leg_notional`(同步派生,消除 0 vs 0.02 矛盾);`hedge_timeout_by_timeframe = {5m:5,15m:10,1h:20,4h:30}`(新 key,替代平坦 20s)。
4. **oracle_direction gate 对 maker 启用**(:2063-2069 的豁免删除)。
5. **`reentry_cooldown_seconds_per_market` 默认 0→5**;`min_oracle_move_pct` 两处默认(0.15/0.30)统一为一处 config;熔断入 config;懒 snapshot;缓存(同 B1.8)。

**验收**:skew 单测(cap、方向、加权和=1);组合成本闸边界;seconds_left 撤单窗口;`maker_quote_*` 全 key grep≥2。

## WO-B4 五个小策略

1. **`crypto_5m_midcycle`**:(a) 真费用——EV/ROI 减 `taker_fee_pct(vwap)`,删除 `fee=0.0`/`skip_fee_model`(:259/560);(b) `win_prob_estimate=0.80`(:517)入 config,且当 `row["oracle_history"]` 可用时改用 `prob_above`(经 digital_sigma 的 `_realized_vol_per_sec` 抽共享)估 p,fallback 才用配置常数;(c) CycleTracker 300.0 三处常数 → `timeframe_seconds` 推导。
2. **`crypto_digital_sigma_edge`**:(a) 平坦 `fee_buffer=0.015` → 每侧 `taker_fee_pct(cost)`,`fee_buffer` 保留默认 **0.0** 作额外 margin(schema 说明语义变化);(b) oracle age 走 per-tf helper(现平坦 4000 却跑 5m+15m);(c) `_realized_vol_per_sec` 按 `(market_id, len(history), last_ts)` 缓存;(d) `recent_move_zscore` 缺失默认 0.0 的 fail-open(:388)→ 缺失时按配置拒或降 confidence。
3. **`crypto_spike_reversion`**:(a) `_score_market` 补显式 `edge ≥ min_edge_percent` 闸(现仅 fee-clearance,:356);(b) rejection 单遍化——`_rejection_reason` 删除,原因从 scoring pass 的 GateResult 导出(修 :532 裸 oracle、:545 硬编码 300s);(c) confidence clamp 下限 0.44→0.30;(d) `liquidity_cap_fraction` evaluate 里 0.07 硬数(:790)改读 config。
4. **`crypto_entropy_maker`**:(a) `min_entropy` 默认 0.82→**0.0**(注释数学互斥:H(0.80)=0.722,与 min_entry_price=0.80 不可同真;key 保留可调);(b) confidence clamp 下限 0.40→0.30;(c) rejection 单遍化(同 spike);(d) `min_orderflow_alignment/min_recent_move_zscore/cancel-recovery` 三个"软加分"参数的 schema description 明确写"bonus 阈,非过滤器"。
5. **`crypto_distance_edge`**:(a) 新 key `distance_cost_tiers_bps`(优先于 USD tiers;换算注释:USD tiers 绑定 BTC 价格 regime);(b) EV 闸——`win_prob` 有 oracle_history 时用 `prob_above`,并要求 `EV>0` 才 emit(修「默认负 EV 靠下游过滤」);`edge_uplift_pct` 保留;(c) `min_seconds_to_resolution` 10→60(走 helper)。

**验收**:每策略 2–4 个单测覆盖上述点;费用统一 grep(全部经 kelly 曲线);midcycle/digital 无 `skip_fee_model`/平坦 buffer 残留。

---

# 全局验收清单(我在 GitHub 上逐条检查的内容)

1. `python3 -m py_compile` 全部触碰文件通过(CI 或本地日志)。
2. 死配置清零:对每个策略文件跑
   `for k in $(python3 -c "print(' '.join(DEFAULT_CONFIG.keys()))")...`(或人工)确认每 key ≥2 处出现。重点:`directional_*`、`convergence_*`、`maker_quote_*` 三块。
3. 新增单测全绿:`pytest backend/tests/test_*_repair.py`。
4. 行为变更表在 PR 描述中(默认值旧→新)。
5. 无新依赖(`git diff` 不含 requirements/pyproject 变更)。
6. 合并后 DB `strategies.source_code` 同步计划写明(哪些 slug 要推)。
7. Shadow A/B:先 shadow 跑修复版 ≥3 天,对照指标——信号量、选择率、per-strategy PnL、拒单原因分布(修复后 `rejection_reason` 才可信)。

# 风险与顺序建议

- 先 WO-B0(共享层)→ B1/B2/B3 可并行(不同文件)→ B4;批次 A 五个工单相互独立可并行。
- B2(convergence 改回文档语义)是**行为大改**:旧 latency 行为由 directional 承担,若你线上依赖旧 convergence 行为,合并前先在 shadow 对照确认。
- 所有默认值变化只影响新装;线上生效取决于 DB 参数——同步时注意别把用户已调好的参数覆盖掉(只推代码,参数走 migration/人工核对)。
